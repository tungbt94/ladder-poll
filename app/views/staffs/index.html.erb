<% provide(:title, 'All Staff') %>
<h1>All Staff</h1>
  <% if current_staff%>
    <%= will_paginate%>
      <table width=100%>
        <% @staffs.each do |staff|%>
          <tr width=100%>
            <td width=20%>
              <%= image_tag staff.avatar.url(:thumb) %>
            </td>
            <td width=80%>
              <%= link_to staff.name, staff %>
            </td>
          </tr>
        <% end %>
      </table>
    <%= will_paginate%>
	<% else %>
  	<%= link_to "Sign in now!", "/auth/google_oauth2" , id: "sign_in", class: "btn btn-lg btn-primary" %>
  <% end %>
