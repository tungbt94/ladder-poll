<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>
        <% if logged_in? %>
          <li>
            <%= link_to "Poll", polls_path %>
          </li>
          <li>
            <%= link_to "Staff", staffs_path %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= @current_staff.name %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", @current_staff %></li>
              <li><%= link_to "Settings", staff_edit_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path %>
              </li>
            </ul>
          </li>
        <% else %>
          <%= link_to "Log in", staff_login_path, class: "btn btn-primary"%>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
